import 'package:premium_pay_new/export_files.dart';

class NovayaZayavka extends StatelessWidget {
  const NovayaZayavka({super.key});

  @override
  Widget build(BuildContext context) {
    
    return SafeArea(
      child: const Scaffold(
        body: Center(
          child: Text('Novaya Zayvaka'),
        ),
      ),
    );
  }
}
